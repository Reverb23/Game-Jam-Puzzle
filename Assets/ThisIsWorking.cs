using UnityEngine;

public class ThisIsWorking : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("LOLOLOLOLOLOL!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
